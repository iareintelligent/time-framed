import React from "react";
import DayViewComponent from "./DayViewComponent";

const Timeline = () => (
    <section>
        <DayViewComponent />
        <DayViewComponent />
        <DayViewComponent />
    </section>
);

export default Timeline;
